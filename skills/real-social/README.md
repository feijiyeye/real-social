# 真实社交 Skill

这是一个可迁移的 Codex Skill 快照。入口文件是 `SKILL.md`，运行时资源位于
`runtime/`、`references/`、`scripts/` 和 `knowledge/`。

先运行 `python3 scripts/validate_bundle.py .` 检查包完整性，再开始使用。

本目录的当前版本为 `0.1.0-private`。知识快照包含原始聊天、课程和话术来源，
仅供获得授权的接收者使用；不要在未经审查和授权的情况下公开或再分发。
